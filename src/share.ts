export interface totalBuy {
  nameTicket: string;
  value: number;
}

export interface currentBus {
  number: string;
  name: string;
  distance: number;
  minutes: number;
  valueS: number;
  valueL: number;
}
